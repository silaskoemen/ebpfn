# @package _global_

mode:
  name: audit
repeats: 5
max_rows: 640
# Design-point width: applicability_max_features=100 (map dims 100/800/1056/356). Real tasks
# truncate to numeric_names[:max_features], so this must reach 100 for the coordinate vectors -
# not just the p-complexity cost probe - to exercise the certified dimension.
max_features: 100
