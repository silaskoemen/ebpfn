# @package _global_
mode:
  name: audit
  output_dir: benchmarks/results/pfn/audit
  smoke_steps: 200
  # Decision cells: anchor and the realized upper row quantile / frozen maximum context.
  # Use the production feature width and task batch rather than the smoke-model grid.
  profile_rows: [640, 1024]
  profile_features: [100]
  profile_tasks: 1
