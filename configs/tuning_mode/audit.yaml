# @package mode
name: audit
output_dir: benchmarks/results/tuning/audit_prior_distance
repeats: 8
n_probe_fit: 512
n_probe_score: 256
n_features: 20
n_real_tasks: 1
# Cloud-size sensitivity and closest-to-baseline were already measured by the
# earlier audit. This confirmation isolates the changed D3 penalty at the frozen
# claim-bearing cloud size.
cloud_sizes: [256]
regularization_policies: [none, prior_distance]
